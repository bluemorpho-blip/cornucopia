<p> Here are your Cornucopias, <%= @user.username %>
  <% @user.cornucopias.each do |corn| %>
    <ul>
      <li>A href="/cornucopias/<%= corn.id %>"><%= corn.name %></a></li>
    </ul>
  <% end %>
<div class="delete">
  <form action="/cornucopias/<%= @cornucopia.id %>" method="POST">
    <input type="hidden" name="_method" value="delete">
    <input id="submit" type="submit">
  </form>
</div>
