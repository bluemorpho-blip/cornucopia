<div class="crud">
  <h1>Stuff Something In Your Cornucopia</h1>

  <form action="/items" method="POST">
    <label> Item Name: </label>
    <input type="text" name="name">

    <br />

    <label> Which Cornucopia does this belong in? </label>
    <select name="cornucopia_id">
      <% current_user.cornucopias.each do |cornucopia| %>
        <option value=<%=cornucopia.id %>><%= cornucopia.name %></option>
        <% end %>
      </select>

      <input class="btn btn-primary" type="submit" value="Create">
    </form>
  </div>
